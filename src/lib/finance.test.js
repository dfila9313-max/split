import {describe,expect,it} from 'vitest'
import {calculateBalances,simplifyDebts} from './finance'
const members=[{id:'a'},{id:'b'},{id:'c'}]
describe('financial engine',()=>{it('splits expenses equally and rounds to cents',()=>{const g={members,expenses:[{payerId:'a',amount:100,participantIds:['a','b','c']}]};expect(calculateBalances(g)).toEqual({a:{paid:100,owed:33.33,net:66.67},b:{paid:0,owed:33.33,net:-33.33},c:{paid:0,owed:33.33,net:-33.33}})});it('simplifies a debt chain',()=>{const g={members,expenses:[{payerId:'b',amount:20,participantIds:['a']},{payerId:'c',amount:20,participantIds:['b']}]};expect(simplifyDebts(g)).toEqual([{fromId:'a',toId:'c',amount:20}])});it('supports selected participants only',()=>{const g={members,expenses:[{payerId:'a',amount:30,participantIds:['a','c']}]};const b=calculateBalances(g);expect(b.b.owed).toBe(0);expect(b.c.net).toBe(-15)});it('ignores malformed expenses without participants',()=>{const g={members,expenses:[{payerId:'a',amount:30,participantIds:[]}]};expect(simplifyDebts(g)).toEqual([])})})
